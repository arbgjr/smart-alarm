import React from 'react';

interface PwaUpdaterProps {}

const PwaUpdater: React.FC<PwaUpdaterProps> = () => {
  // PWA functionality is temporarily disabled
  // Enable when PWA dependencies are properly configured
  return null;
};

export { PwaUpdater };
